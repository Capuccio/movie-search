/** @format */

import styled from 'styled-components'

export const BillboardContainer = styled.div`
	transition: 0.4s;
	&:hover {
		cursor: pointer;
		border-radius: 10px;
		box-shadow: 0px 0px 6px rgba(97, 97, 97, 0.6);
	}
`

export const billboardImg = {
	width: '140px',
	borderRadius: '10px',
}
